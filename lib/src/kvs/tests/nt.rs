#[tokio::test]
#[serial]
async fn remote_notifications_happen_before_local_key_nt() {
	// Create bootstrap - node 1
	// Create bootstrap - node 2
	// Create live query
	// Create remote notification entry
	// Verify create scans, deletes, publishes notifications before local notification
}
